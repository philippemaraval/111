{
  "name": "111-social-autopilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute 111-social --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
